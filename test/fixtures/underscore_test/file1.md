Testing grunt-md2html with underscore templating
=================================================

The output should be done in one single HTML file:

<%= destination %>

This one was created on: <%= grunt.template.today('yyyy-mm-dd HH:MM:ss') %>

Input file: <%= src %>
